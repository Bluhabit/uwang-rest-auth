version: '3.5'

services:
  uwang-rest-api:
    container_name: "uwang-rest-api"
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    expose:
      - 8000
    networks:
      - app
#  postgres:
#    image: postgres:14.3
#    environment:
#      POSTGRES_USER: bluehabit
#      POSTGRES_PASSWORD: bluehabit
#      POSTGRES_DB: bluehabit
#    ports:
#      - "5432:5432"
#    restart: always
  pgAdmin:
    image: dpage/pgadmin4
    container_name: pgAdmin
    env_file:
      - ./.env
    ports:
      - "5050:80"

networks:
  app:
    external: true